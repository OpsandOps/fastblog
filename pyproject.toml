[project]
name = "fastapi-blog"
version = "0.1.0"
description = "a blog created with python fastapi"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.139.2",
    "ipython>=9.15.0",
    "sqlalchemy>=2.0.51",
]
